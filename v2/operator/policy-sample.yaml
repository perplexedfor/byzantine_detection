apiVersion: security.example.com/v1
kind: ByzantinePolicy
metadata:
  name: strict-protection
  namespace: default
spec:
  # Controls whether the operator will automatically uncordon 
  # nodes when their ML trust score recovers.
  # Set to 'false' if you want a human to manually inspect flagged nodes.
  autoRemediate: true
